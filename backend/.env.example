# =============================================================================
# QUATARLY - Environment Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# NEVER commit .env file to version control!

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection string
# Format: postgresql+asyncpg://username:password@host:port/database
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/quatarly

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# Generate a secure secret key using: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-change-this-in-production

# JWT Algorithm (don't change unless you know what you're doing)
ALGORITHM=HS256

# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# JUDGE0 API CONFIGURATION (Optional - for code grading)
# =============================================================================
# Get API key from: https://rapidapi.com/judge0-official/api/judge0-ce
JUDGE0_API_KEY=your-judge0-api-key-here
JUDGE0_API_HOST=judge0-ce.p.rapidapi.com

# =============================================================================
# EMAIL CONFIGURATION (SMTP)
# =============================================================================
# See SMTP_SETUP_GUIDE.md for detailed setup instructions

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP Authentication
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# Email Sender Information
SMTP_FROM_EMAIL=noreply@quatarly.com
SMTP_FROM_NAME=Quatarly

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================
# Uncomment and configure if needed

# CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Environment (development, staging, production)
# ENVIRONMENT=development

# Debug mode (true/false)
# DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO
